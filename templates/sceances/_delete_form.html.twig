<form method="post" action="{{ path('app_sceances_delete', {'id': sceance.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sceance.id) }}">
    <button class="btn">Delete</button>
</form>
