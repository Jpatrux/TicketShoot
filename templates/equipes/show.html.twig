{% extends 'base.html.twig' %}

{% block title %}Equipes{% endblock %}

{% block body %}
    <h1>Equipes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ equipe.id }}</td>
            </tr>
            <tr>
                <th>NomClub</th>
                <td>{{ equipe.nomClub }}</td>
            </tr>
            <tr>
                <th>Categorie</th>
                <td>{{ equipe.categorie }}</td>
            </tr>
            <tr>
                <th>Couleur1</th>
                <td>{{ equipe.couleur1 }}</td>
            </tr>
            <tr>
                <th>Couleur2</th>
                <td>{{ equipe.couleur2 }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_equipes_index') }}">back to list</a>

    <a href="{{ path('app_equipes_edit', {'id': equipe.id}) }}">edit</a>

    {{ include('equipes/_delete_form.html.twig') }}
{% endblock %}
